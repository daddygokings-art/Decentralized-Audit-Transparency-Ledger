- [ ] Add batch Merkle root storage key `BatchMerkleRoot(batch_index)`
- [ ] Implement Merkle root computation helper and store in `log_events`
- [ ] Add `get_batch_merkle_root` and `verify_batch` (recompute root from provided events)
- [ ] Add unit tests for batch logging + verify (correct, tampered, single-event)
- [ ] Run `cargo test` to ensure all tests pass

