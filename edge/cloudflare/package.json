{
  "name": "@audit-ledger/cloudflare-edge-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for low-latency event ingestion and query caching",
  "main": "worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240101.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
